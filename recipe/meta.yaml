{% set name = "hydrodata" %}
{% set version = "0.9.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: c5e0bc6a09a540f337f74098fd9509e2b4e309043f7664c0e18c7210647cb9ad

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools_scm
  run:
    - python >=3.6
    - matplotlib-base
    - folium
    - py3dep
    - pydaymet
    - pygeoogc
    - pygeoutils
    - pynhd

test:
  imports:
    - hydrodata

about:
  home: https://github.com/cheginit/hydrodata
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A portal to access hydrology and climatology data

  description: |
    Hydrodata is a python library designed to aid in watershed analysis.
    It provides easy and consistent access to a handful of hydrology and
    climatology databases with some helper functions for visualization.
  doc_url: https://hydrodata.readthedocs.io
  dev_url: https://github.com/cheginit/hydrodata

extra:
  recipe-maintainers:
    - cheginit
